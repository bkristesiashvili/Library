@model GenreListViewModel
<div>
    <div>
        <form onsubmit="return false;" method="post" data-restore-genre="@Model.Id"
              asp-controller="@GenreIndexLink"
              asp-action="restore" enctype="application/x-www-form-urlencoded">
            <input asp-for="Id" />
            <button type="button" onclick="genreRestore('@Model.Id')" class="btn btn-sm text-11px text-success">
                <i class="fas fa-trash-restore"></i>
            </button>
        </form>
    </div>
</div>